# 🤖 تثبيت وتشغيل Ollama

## المشكلة الحالية
```
cURL error 7: Failed to connect to localhost port 11434
```
هذا يعني أن Ollama غير شغال أو غير مثبت.

---

## ✅ الحل: تثبيت وتشغيل Ollama

### 1. تحميل Ollama

**للـ Windows:**
1. افتح الرابط: https://ollama.com/download/windows
2. حمّل ملف `OllamaSetup.exe`
3. شغّل الملف واتبع خطوات التثبيت

**أو باستخدام PowerShell:**
```powershell
# Download and install
irm https://ollama.com/install.ps1 | iex
```

---

### 2. تشغيل Ollama

بعد التثبيت، افتح **Command Prompt** أو **PowerShell** وشغّل:

```bash
ollama serve
```

**يجب أن ترى رسالة:**
```
Listening on 127.0.0.1:11434
```

---

### 3. تحميل Model (Gemma 2B)

**في نافذة CMD جديدة** (اترك الأولى شغالة)، شغّل:

```bash
ollama pull gemma:2b
```

**سيبدأ التحميل:**
```
pulling manifest
pulling 8ccb135d64b4... 100% ▕████████████▏ 1.6 GB
pulling 966de95ca8a6... 100% ▕████████████▏   485 B
pulling faffde1eaecb... 100% ▕████████████▏   342 B
pulling 1d3d775a0bf5... 100% ▕████████████▏   106 B
pulling f02dd72bb242... 100% ▕████████████▏    59 B
verifying sha256 digest
writing manifest
removing any unused layers
success
```

**انتظر حتى ينتهي التحميل (حوالي 1.6 GB)**

---

### 4. اختبار Ollama

بعد التحميل، اختبر أن الـ model يعمل:

```bash
ollama run gemma:2b "مرحباً"
```

**يجب أن ترى رد من الـ AI**

---

### 5. التحقق من عمل Ollama API

**في CMD:**
```bash
curl http://localhost:11434/api/tags
```

**يجب أن ترى:**
```json
{
  "models": [
    {
      "name": "gemma:2b",
      "model": "gemma:2b",
      ...
    }
  ]
}
```

---

## 🚀 استخدام التطبيق الآن

بعد تشغيل Ollama:

1. **تأكد أن Ollama شغال:**
   ```bash
   ollama serve
   ```
   (اترك هذه النافذة مفتوحة)

2. **افتح المتصفح:**
   ```
   http://127.0.0.1:8000/wizard/start
   ```

3. **اضغط "توليد بالذكاء الاصطناعي"**
   - الآن سيعمل بدون أخطاء!

---

## 📝 Models المتاحة

يمكنك استخدام models أخرى:

### Models صغيرة (سريعة):
```bash
ollama pull gemma:2b      # 1.6 GB - سريع جداً
ollama pull phi3:mini     # 2.3 GB - جيد للنصوص القصيرة
ollama pull llama3.2:1b   # 1.3 GB - الأخف والأسرع
```

### Models متوسطة (موازنة):
```bash
ollama pull llama3.2:3b   # 2.0 GB - ممتاز للنصوص الطويلة
ollama pull mistral:7b    # 4.1 GB - قوي ومتوازن
```

### Models كبيرة (دقة عالية):
```bash
ollama pull llama3:8b     # 4.7 GB - جودة ممتازة
ollama pull llama3:70b    # 40 GB - الأفضل (يحتاج GPU قوي)
```

---

## ⚙️ تغيير الـ Model في التطبيق

**افتح ملف:**
```
wave-app/config/ollama.php
```

**غيّر السطر 18:**
```php
'default_model' => env('OLLAMA_MODEL', 'gemma:2b'),
```

**إلى:**
```php
'default_model' => env('OLLAMA_MODEL', 'llama3.2:3b'),  // أو أي model آخر
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: "connection refused"
**الحل:**
```bash
# تأكد أن Ollama شغال
ollama serve
```

### المشكلة: "model not found"
**الحل:**
```bash
# تحقق من الـ models المثبتة
ollama list

# حمّل الـ model
ollama pull gemma:2b
```

### المشكلة: بطء في التوليد
**الحل:**
1. استخدم model أصغر (gemma:2b أو llama3.2:1b)
2. قلل `max_tokens` في `config/ollama.php`
3. استخدم GPU إذا متوفر

---

## 📊 مقارنة Models

| Model | Size | Speed | Quality | Use Case |
|-------|------|-------|---------|----------|
| llama3.2:1b | 1.3 GB | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | نصوص قصيرة |
| gemma:2b | 1.6 GB | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | متوازن |
| llama3.2:3b | 2.0 GB | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | خطط أعمال |
| mistral:7b | 4.1 GB | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | نصوص طويلة |
| llama3:8b | 4.7 GB | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | جودة عالية |

---

## 🎯 التوصية

**للبدء السريع:**
```bash
ollama pull gemma:2b
```

**للجودة الأفضل:**
```bash
ollama pull llama3.2:3b
```

---

## ✅ الخطوات النهائية

1. ✅ حمّل Ollama من https://ollama.com/download
2. ✅ ثبّت Ollama
3. ✅ شغّل `ollama serve`
4. ✅ حمّل model: `ollama pull gemma:2b`
5. ✅ اختبر: `ollama run gemma:2b "مرحباً"`
6. ✅ استخدم التطبيق!

---

## 🆘 الدعم

إذا واجهت مشاكل:
- Ollama Docs: https://ollama.com/docs
- GitHub: https://github.com/ollama/ollama

**ملاحظة:** اترك نافذة `ollama serve` مفتوحة طول فترة استخدام التطبيق.
